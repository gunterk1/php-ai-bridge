/vendor/
/var/
.env.local
.phpunit.result.cache

# Regenerated on every container build — 1300 lines of IDE type hints that would
# churn in every diff without telling anyone anything.
/config/reference.php
